import { IconAngle } from "@tabler/icons-react";

export default function Home() {
  return (
    <div
      className={`flex justify-center items-center bg-black text-white text-6xl h-screen
    `}
    >
      <IconAngle size={100} strokeWidth={0.5}/>
      Fundamentos
    </div>
  );
}
